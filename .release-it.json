{
  "git": {
    "requireBranch": "v2",
    "commitMessage": "chore: release v${version}",
    "publish": true,
    "requireCommits": true,
    "requireCleanWorkingDir": false
  },
  "github": {
    "release": true
  },
  "npm": { "publishArgs": ["--access public", "--provenance", "--tag v2-lts"] },
  "hooks": {
    "before:init": ["git pull origin v2", "npm run lint", "npm run test"],
    "before:bump": ["npm run build", "npm run test:modules"],
    "after:github:release": "bash scripts/gh-latest-release-tag.sh ${version}"
  },
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "angular",
      "ignoreRecommendedBump": true,
      "gitRawCommitsOpts": {
        "merges": null
      },
      "parserOpts": {
        "mergePattern": "^Merge pull request #(\\d+) from (.*)$",
        "mergeCorrespondence": ["id", "source"]
      }
    }
  }
}
