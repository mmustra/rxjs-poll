/**
 * Timing tolerance in milliseconds to compensate for JavaScript timing inaccuracies.
 * Used when calculating pause delays in document visibility pause/resume logic.
 */
export const timingToleranceMs = 100;
